import React from 'react';
import './footer.styles.scss';

const Footer = (props) => {
  return (
    <footer className="pretty-footer" {...props}>
      <p>
        Footer
      </p>
    </footer>
  );
}

export default Footer;
